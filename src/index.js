/**
 * Main pipeline entry point.
 * Ties together all pipeline stages: fetch → rewrite → TTS → assemble → upload.
 */

/**
 * Runs a single end-to-end pipeline execution for the given subreddit.
 *
 * @param {string} subreddit - The subreddit to pull content from.
 * @returns {Promise<void>}
 */
export async function runPipeline(subreddit) {}
